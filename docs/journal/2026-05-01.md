# 2026-05-01 の作業記録

> M1 Mac セッション、4/30 → 5/1 の連続セッション。前夜の温存モード判断 (週次 11% 残) にもかかわらず、午前中から複数のローカル作業が発生。

## サマリー

- **0:02** X 投稿 (cost analysis、4/30 23:39 Substack note の X 版)、初動 11 views/2 ❤️/1 🔖
- **6:15〜** M1 メモリ確認 → agent-browser kill で **1.2 GB 解放**
- **6:30〜** しゅん先生 PC 遠隔操作セットアップ (DHCP IP 変動 192.168.2.174→.187 判明、SSH 設定完遂、`ssh shun-sensei` alias 化)
- **6:50〜** Tailscale 導入は見送り、LAN 内アクセスで十分と判断
- **7:00〜** Joy-Con 2 Windows マウス化ドライバ開発計画策定 + しゅん先生 PC に VS Code/.NET 8/C# Dev Kit/gh CLI インストール、JoyCon2Mouse プロジェクト雛形作成 (commit `8d0b434` + `27d9e6c`)
- **7:40〜** BT ROUND キーボード (KOR-05、Bluetooth 5.0、単 4 × 2) を しゅん先生 PC にペアリング成功
- **8:30〜** sniffnet 1.5.0 を M1 に brew install (38.9 MB、Rust + iced)
- **11:45〜** **Google日本語入力動作確認** — 4/30 午後の「macOS Tahoe では不可能」結論を**完全撤回**

## 主要イベント

### しゅん先生 PC 遠隔操作セットアップ

#### IP 変動の発見
- 4/30 16:50 時点: 192.168.2.174 (写真共有時)
- 5/1 6:30 時点: ARP 表で 192.168.2.187 (MAC `d4:25:8b:30:f5:d4` で同一機確認)
- DHCP リース更新で IP 変動 = LAN 内固定アクセスは不安定。長期運用は Tailscale or mDNS 化推奨だが今回は見送り。

#### MASU-p55 の LAN IP 判明
- 192.168.2.248 = `~/.ssh/known_hosts` に旧鍵あり、ping/SSH 22 通る
- SSH 試行で `gci_admin@masu-p55` 認証通過 → **MASU-p55 = 192.168.2.248** (Tailscale 100.125.21.47 の他に LAN 経路もある) と確定
- 既存の `wirelessml@gmail.com` Tailnet で接続済の Windows 機。今後 LAN 内では `100.125.21.47` か `192.168.2.248` どちらでも到達可。

#### しゅん先生 PC SSH 設定 (Claude Code CLI セッション on しゅん先生 PC 経由)
1. ssh-agent サービス Disabled → Manual + Running に変更
2. **OpenSSH Server 別途インストール + 起動** (これが本命、ssh-agent は client、sshd は server)
3. ファイアウォール TCP 22 許可ルール初期は Private only → **Profile=Any に修正**(これが詰まりの原因、外部から到達できなかった)
4. `C:\ProgramData\ssh\administrators_authorized_keys` に M1 公開鍵 (`ssh-ed25519 ... yuika@macbook-air-claude-desktop`) 登録 (Windows OpenSSH の `Match Group administrators` 仕様、`~/.ssh/authorized_keys` ではない)
5. M1 から SSH 接続成功:
   - ホスト名: **DESKTOP-ATQ36KS**
   - ユーザー: **wirel** (Administrators グループ所属)
   - OS: Microsoft Windows 11 Home **Insider Preview** 10.0.26300
   - インターフェース名: **`irukasensei`** (いるか先生のローマ字命名、可愛い)
6. M1 `~/.ssh/config` に alias 追加 (`ssh shun-sensei` で接続可)
7. `docs/machines/shun-sensei-pc.md` に SSH 接続情報セクション追加 (commit `0f3097f`)

⚠ **パスワード**: `wirel` ユーザーの password は会話中に共有されたが git に記録せず。今後は鍵認証メインで運用。

### Joy-Con 2 Windows マウス化ドライバ開発計画

#### きっかけ
ユーザーが maruta/joycon2-usb-presenter (Apache 2.0、Switch 2 Joy-Con BLE プロトコル解析済の nRF52840 dongle ファームウェア) を共有。「dongle 不要で純粋ソフト実装したい」「Windows でやりたい」「VS Code で」と開発要件が固まる。

#### 計画策定
`docs/projects/joycon2-windows-driver.md` (157 行、commit `01757f0` + VS Code 路線への変更で `5aec372`):

- 5 Phase マイルストーン: 情報整理 → BLE 接続 → マウス注入 → GUI → 配布
- 想定トークン: Opus 4.7 ~170k + Sonnet ~30k = 1 リセット枠 + 別日跨ぎ規模
- Substack 連載化シナリオ (5 回構成、4/28-5/10 SEO ピーク中)
- 開発環境: VS Code + .NET 8 SDK + C# Dev Kit + WinRT BLE + WPF or Avalonia UI

#### 物理準備 (Claude 不使用、SSH 経由で M1 から remote install)
しゅん先生 PC で winget + extension コマンドで一括インストール:
- VS Code 1.118.1 ✓
- .NET 8 SDK 8.0.420 ✓
- C# Dev Kit 3.10.14 ✓
- C# extension 2.130.5 ✓
- GitLens 17.12.2 ✓
- gh CLI 2.92.0 ✓

#### プロジェクト雛形
`C:\Users\wirel\source\repos\joycon2-mouse-windows\`:
- .NET 8 console (TargetFramework: `net8.0-windows10.0.19041.0`、WinRT BLE API アクセス用)
- git init + 2 commits (initial scaffold + TargetFramework fix)
- maruta reference を隣の `joycon2-usb-presenter\` に clone (Phase 1 情報整理用)

git config user.name=wirel / user.email=wirelessml@gmail.com も設定済。

#### 残作業 (5/2 以降)
- `gh auth login` (対話式、ブラウザ認証要)
- `gh repo create wirelessml/joycon2-mouse-windows --private --source=. --push`
- Joy-Con 2 充電 + Switch 2 から切断 + ペアリングモード確認 (物理)

### BT ROUND キーボード (KOR-05) ペアリング

- 製品: コーリュウ株式会社「丸型ワイヤレスキーボード」、Bluetooth 5.0 / HID プロファイル
- 単 4 × 2 本 (別売)、連続動作 80 時間、84 キー、約 33 × 14 × 2.6 cm、ABS、技適 210-209936
- ペアリング手順:
  1. Fn + E で **Windows モード**に切替 (LED 3 回点滅)
  2. Fn + C を 3 秒長押し → ペアリングモード (LED 点滅)
  3. しゅん先生 PC 側「設定 → Bluetooth → デバイス追加」→ 「BT ROUND」選択
  4. PIN 求められれば 0000 (今回不要だった)
- 接続確認: しゅん先生 PC の `Get-PnpDevice -Class Bluetooth` に **BT ROUND OK** 表示
- 注意: 一度に 1 台のみ、電源 ON/OFF なし (使わない時は電池抜く)、10 分無操作スリープ

### sniffnet 1.5.0 (M1 Mac)

`brew install sniffnet` で 38.9 MB、Formula で `/opt/homebrew/bin/sniffnet`。Rust + iced GUI のクロス PF ネットワークモニター。

検証は明日以降の TODO に追加 (CLAUDE.md 今週優先 TODO に追記、commit `b1f1729`)。Joy-Con 2 BLE 開発の補助としては不適 (IP only、BLE は見えない、PacketLogger 推奨)。

### 🚨 4/30 午後の「Google日本語入力切替不可」結論を完全撤回

#### きっかけ
LIAM (@liappleq_1283) のツイート「俺が MacBook 買って 1 番最初に入れるものこれ」(4/30 19:09、38 ❤️ / 1,096 views) に Google日本語入力の画像。4/30 午後の自分の検証結果との矛盾を確認すべく Web 検索。

#### Web 検索で判明
- Google公式 Gboard コミュニティに「Google日本語入力が macOS 26 で正常に動作しません」スレッド存在 (内容は WebFetch でナビ枠のみ取得)
- 北村事務所ブログ等で「**インストール後に Mac を再起動**」と明記
- v3.33 から表示名は「**ひらがな (Google)**」(かっこ内表記、4/30 検証で「Google」検索ヒット 0 だった理由)

#### 動作確認 (5/1 11:45 頃)
- Mac 再起動後、System Settings → キーボード → 入力ソース → 「+」→ 日本語 → **「ひらがな (Google)」** 追加 → ✓ 動作
- メニューバーに「あ ひらがな (Google)」アクティブ、ショートカット ⌃⇧J/K/L/; 等利用可
- Google IME メニュー (再変換 / 環境設定 / 単語登録 / 辞書ツール) 全機能アクセス可
- Apple ことえり (`日本語 - ローマ字入力`) を「-」で削除、卒業完了

#### 真因と訂正

| 4/30 午後の結論 (誤) | 5/1 朝の事実 (正) |
|---|---|
| 「macOS 26.5 Tahoe + Google IME 3.33 は三重ブロックで不可能」 | **再起動さえすれば普通に動く** |
| System Settings UI に「Google」が出ない | 再起動前の未登録状態だった、表示名は「ひらがな (Google)」 |
| TIS API `-50 paramErr` でブロック | 未登録状態への正しいエラー応答だった可能性 |
| defaults plist 直書きを OS が即 revert | 入力ソースとして登録されてないので OS が拒否したのが正しい |

→ **真因は「再起動を試さなかった」一点**。三重ブロックは全部「再起動前の未登録状態を観察していた」だけ。journal 4/30 追補 1.4 の「Google日本語入力切替不可」結論は撤回。

#### 学んだこと
- macOS の入力ソース登録は **ファイル配置 + 再起動 = 反映** という古典的パターン
- v3.33 で表示名が「Google日本語入力」→「ひらがな (Google)」等に変わった (4 モード並列表示)
- 「不可能」の結論を出す前に、**OS のお決まりの儀式 (再起動)** を試すべきだった
- Web 検索で公式コミュニティ + サードパーティブログを 1 つでも当たっていれば最初から判明した問題

## 完了した作業

- [x] 0:02 X 投稿 (cost analysis、Substack note の X 版)
- [x] M1 メモリ削減 (agent-browser kill で 1.2 GB 解放)
- [x] しゅん先生 PC SSH リモートアクセス確立 (`ssh shun-sensei`)
- [x] MASU-p55 LAN IP 確定 (192.168.2.248)
- [x] Joy-Con 2 ドライバ開発計画策定 + 環境構築 (しゅん先生 PC に VS Code + .NET 8 + C# 拡張 + gh CLI、プロジェクト雛形 + maruta reference clone)
- [x] BT ROUND キーボード ペアリング (しゅん先生 PC)
- [x] sniffnet 1.5.0 M1 install
- [x] Google日本語入力 動作確認 (4/30 午後の結論撤回)
- [x] Apple ことえり (RomajiTyping) 削除、Google IME 完全移行
- [x] CLAUDE.md 今週優先 TODO 整備 (Joy-Con 2 + sniffnet 追加、Substack publish 削除)

## 残った TODO (5/2 以降)

- [ ] **Joy-Con 2 ドライバ Phase 1 着手** (情報整理、maruta コード解析、`docs/projects/joycon2-windows-driver/protocol-spec.md` 作成)
- [ ] gh auth login (しゅん先生 PC、対話式、要ブラウザ)
- [ ] GitHub repo 作成 + push (`gh repo create wirelessml/joycon2-mouse-windows`)
- [ ] Joy-Con 2 物理準備 (充電 / Switch 2 切断 / ペアリングモード確認)
- [ ] sniffnet 実機検証 (M1 でコワーキング Wi-Fi 観察等)
- [ ] LIAM さんツイートに @minimalistneko リプライ (Google IME 訂正報告 + 再起動が答え情報共有、誠実さの観点で早めに)
- [ ] Substack note (cost analysis) の URL 取得 → 4/30 journal 追補 1.5 に追記

## Substack note + X 投稿の今日の振り返り

- **昨夜の cost analysis 投稿** = 概念整理として価値あり、引き続き反応観察 (24h 後 = 5/2 早朝)
- **訂正案件**: Google IME 不可結論を 4/30 journal で公開 push 済 (commit `cbddf7a` + `c82f934`)。X / Substack 投稿はしてなかったので幸い外部訂正は不要、ただし journal 上の訂正は誠実さの観点で実施 (この journal がそれ)

## 関連ファイル

- 4/30 journal: `docs/journal/2026-04-30.md` (追補 1.4 末尾の「Google日本語入力切替不可」記述は本日訂正、追補 1.6 として末尾にリンクを追加予定 or 本ファイルへのポインタ)
- しゅん先生 PC: `docs/machines/shun-sensei-pc.md` (SSH 接続情報セクション、commit `0f3097f`)
- Joy-Con 2 プロジェクト計画: `docs/projects/joycon2-windows-driver.md` (commit `01757f0` + `5aec372`)
- 引き継ぎドキュメント (4/30): `docs/journal/2026-04-30-handoff-shun-sensei-pc.md`

## 追補 (12:20〜12:45 JST) — Codex CLI external_migration 機能を実機検証 + AGENTS.md 誤置換問題発覚 → シンボリックリンク化

### きっかけ

akihiro (@akihiro_genai、認証済) の 5/1 08:32 ツイート (98 ❤️ / 4,020 views / 89 🔖) で **Codex CLI に Claude Code セッション + 設定取り込み機能が追加**されたとの情報。`~/.codex/config.toml` に `[features] external_migration = true` を書くと初回ディレクトリで案内が出る仕様。

### セットアップ

1. M1 の Codex CLI バイナリを発見: `/Applications/Codex.app/Contents/Resources/codex` (Codex CLI v0.122.0-alpha.1)
2. `~/.local/bin/codex` にシンボリックリンクで PATH 通す
3. `~/.codex/config.toml` に `[features] external_migration = true` 追記
4. テスト用ディレクトリ `~/Desktop/codex-test-migration/` 作成

### 注意: Codex.app GUI ≠ Codex CLI

最初に GUI 起動を試みたが、`/Applications/Codex.app` は ChatGPT デスクトップアプリ (chatgpt.com の wrapper + Computer Use 自動化) で、akihiro さんが言ってたのは **アプリ内のバイナリを CLI として呼び出した時**だけ動く migration 機能。GUI 側からは表示されない。

→ ターミナル.app から `cd ~/Desktop/codex-test-migration && codex` で起動して TUI モード入る、これが正しい動線。

### Migration プロンプトの中身

検出された移行候補 6 項目 (全部 [x] チェック済):

| # | 項目 | 移行先 |
|---|---|---|
| 1 | `~/.claude/settings.json` | `~/.codex/config.toml` |
| 2 | MCP servers (peekaboo / playwright) | `~/.codex/config.toml` |
| 3 | hooks | `~/.codex/hooks.json` |
| 4 | enabled plugins (claude-obsidian-marketplace / claude-plugins-official / thedotmack / ui-ux-pro-max-skill 等 4 marketplaces) | `~/.codex/hooks.json` |
| 5 | recent sessions (`~/.claude/projects/`) | Codex のスレッド一覧へ |
| 6 | **`CLAUDE.md` → `AGENTS.md` (Project: /Users/yuika/Desktop)** | プロジェクトルートに新規 |

`1 (Proceed)` 選択 → 全 6 項目移行成功。Codex 起動 (model gpt-5.5、reasoning effort xhigh)。

### ⚠ AGENTS.md で文字列置換が暴走 (Codex バグ or 仕様)

CLAUDE.md → AGENTS.md コピー時に、Codex が **「Claude」→「Codex」の bulk text replace** を実行。実在のリポ名 / プラグイン名 / スキル名 / GitHub issue 番号まで誤置換:

| 元 (CLAUDE.md) | 置換後 (AGENTS.md、誤) |
|---|---|
| `skills/claude-md-diet/` | `skills/Codex-md-diet/` ← 存在しないパス |
| `claude-obsidian` プラグイン | `Codex-obsidian` ← 存在しないプラグイン |
| `Claude Code 2.1.123` | `Codex 2.1.123` ← バージョン誤記 |
| `anthropics/claude-code#48508` | `anthropics/Codex#48508` ← 存在しない GitHub issue |
| `AgriciDaniel/claude-obsidian#7` | `AgriciDaniel/Codex-obsidian#7` ← 存在しない |
| `Claude Code` | `Codex` (60〜200 ヶ所) | 大半が誤置換 |

→ **AGENTS.md を物理ファイルとして放置すると壊れた状態で git に乗る**。

### 対策: シンボリックリンク化 (commit `999fb8a`)

```bash
rm ~/Desktop/AGENTS.md
ln -s CLAUDE.md ~/Desktop/AGENTS.md
git add AGENTS.md  # mode 120000 で symlink として記録
```

これで:
- ✓ Claude Code / Codex 両エージェントから **同じ CLAUDE.md** を読む状態に
- ✓ 二重管理ゼロ
- ✓ Codex の誤置換が無効化
- ⚠ Windows 側で clone する時は `git config --global core.symlinks true` 必要 (1 回だけ)

### Codex 移行で生成された永続ファイル

| ファイル | 内容 |
|---|---|
| `~/.codex/config.toml` | gpt-5.5 + xhigh + 4 marketplaces + 2 MCP + features.external_migration |
| `~/.codex/hooks.json` | SessionStart hook (inject-usage.sh) |
| `~/.codex/AGENTS.md` | ホームレベル指示 (130 bytes) |
| `~/Desktop/AGENTS.md` | **CLAUDE.md へのシンボリックリンク** (上記対策後) |

### 学んだこと

- **Codex CLI の external_migration は動く**: 設定・MCP・プラグイン・hooks・セッション履歴を自動移行、便利
- **CLAUDE.md → AGENTS.md コピーは bulk text replace バグ**: 「Claude」を機械的に「Codex」へ置換、実在のリソース名まで壊す
- **対策はシンボリックリンク化**: git で `mode 120000` として記録、両エージェントから同じソースが読まれる
- **Codex.app GUI と Codex CLI は別製品**: 名前が同じだが GUI は ChatGPT wrapper、CLI 版が今回試したもの (akihiro さん投稿の対象)
- **Codex のモデル**: gpt-5.5、reasoning effort xhigh = OpenAI の最強コーディング agentic モデル (課金は OpenAI 側、Claude Max とは別)
- **クォータフォールバック路線が完成**: 5/2 リセット前後で Claude 枠枯渇したら Codex に作業移行可能 (Joy-Con 2 ドライバ開発の Phase 2-3 等を Codex で実装する選択肢)

### 5/2 以降の運用方針

- **基本は Claude Code を継続**(Max プラン継続中)
- **Claude 週次枠が逼迫したら Codex にフォールバック**
- **AGENTS.md は CLAUDE.md のシンボリックリンクで永久同期**(物理ファイル化禁止)
- 今後 CLAUDE.md を更新すれば AGENTS.md も自動的に同じ内容に
- `~/.codex/config.toml` の marketplaces / MCP は Claude 側更新時に手で同期 (or 再 migration)

## タグ

#しゅん先生PC #SSH #DHCP変動 #DESKTOP-ATQ36KS #wirel #irukasensei #Windows11InsiderPreview #ssh-agent vs sshd #administrators_authorized_keys #ファイアウォールProfileAny #MASU-p55LAN #JoyCon2 #maruta #VSCode #.NET8 #CSharpDevKit #ghCLI #JoyCon2Mouse #BTROUND #KOR-05 #コーリュウ #sniffnet #Rust #iced #GoogleIME #ひらがなGoogle #macOSTahoe再起動 #Apple ことえり卒業 #4/30結論撤回 #LIAMツイート #cost非対称 #温存モード #Codex #external_migration #akihiro_genai #gpt-5.5 #AGENTS.md #symlink #mode120000 #bulk_replace_bug #Claude→Codex誤置換 #クォータフォールバック
