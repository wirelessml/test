# 2026-06-19 の作業記録

## サマリー

全機 AI CLI 定例更新（updating-ai-cli-fleet スキル）。到達できた **Mac・masu-p55 の2台**を `claude 2.1.181 / codex 0.141.0 / gemini 0.47.0` で統一。しゅん先生 PC は到達不能（電源オフ）で持ち越し。更新前にピン留め記録を確認し、5/20 の codex 0.130.0 ピン留めが**現体制（codex CLI=WSL2 移行済み）では失効**と判断して進めた。

## 更新結果（Before → After）

| マシン | claude | codex | gemini |
|---|---|---|---|
| Mac (M1) | 2.1.181（既に最新） | 0.140.0 → **0.141.0** | 0.46.0 → **0.47.0** |
| masu-p55 | 2.1.179 → **2.1.181**（Win） | WSL2: 0.141.0（既に最新） | 0.46.0 → **0.47.0**（Win npm） |
| しゅん先生 PC | — 到達不能 | — | — |

npm 最新: codex 0.141.0 / gemini 0.47.0。claude は `claude update` のみ使用（`@anthropic-ai` localhost ガードは不変）。

## ピン留め判断の解決（重要・将来の誤更新防止）

- grep で 5/20・5/25 の「Windows codex 0.130.0 ピン留め」を検出。理由は **0.131+ で `codex remote-control` が Unix 専用化＝Windows native で退行**。
- ただし **6/2 に codex CLI は WSL2（Linux）へ移行済み**（masu-p55.md 変更履歴）。WSL2 では remote-control は壊れないため、**このピン留めは現体制では失効**。現に WSL2 codex は既に 0.141.0 で正常。
- よって canonical codex（WSL2）を最新に保つ判断は妥当。ピン留めは「Windows native remote-control を使っていた旧体制」の遺物。

## 気づき（config ドリフト、今回は範囲外で未修整）

masu-p55 に**二重インストール**が2件。今回はバージョン更新のみのルーチン範囲なので未対応、フラグのみ:

1. **codex が2系統併存**: WSL2 `@openai/codex` 0.141.0（＝canonical・委譲先）と、Windows native codex 0.140.0。docs が記載するのは WSL2 のみ。Windows native は用途不明の遺物（旧 remote-control 由来か）。0.140.0 は既に旧ピン 0.130 を超過済み＝ピンは事実上解除済み。
2. **claude が2系統併存**: SSH で走るのは npm-global（`AppData\Roaming\npm\claude`、今回 2.1.181 へ更新）。別に native（`.local\share\claude`）あり。machines doc は `.local\bin\claude.exe`（standalone・毎日0:00タスクで更新）と記載。`claude update` 実行時に「config expects native / running npm-global」警告→config が global に書き換わった。**毎日の自動更新タスクがどちらを更新しているか要確認**（次回コワーキング時 or 余力時）。

## 追記: masup の二重インストール / 旧 remote-control を解決（同日）

ユーザー「masupの問題解決お願いします」→ 上記「気づき」で未対応だった二重インストールを調査の上クリーンアップ。**ユーザー判断＝「WSL2版 remote-control は残す」**。

### 調査で判明した全体像
- claude: 正本=standalone（`.local\bin`、毎日0:00 task `Claude Code Update` が `claude.exe update`）。重複=npm-global `@anthropic-ai/claude-code`（PATH を先取り）。両方 2.1.181。
- codex: 正本=WSL2 0.141.0（`codex exec` 委譲先）。重複=Windows-native npm 0.140.0。
- remote-control タスクが**2系統**: `codex-remote-control`（Windows版、`run-codex-remote.bat`→`codex.cmd remote-control`、**codex 0.131+ で Windows 壊れ済み**＝5/20ピン留めの根拠そのもの）/ `CodexWSLRemoteControl`（WSL2版、**PID 148367 で稼働中**）。`codex-watchdog` は無効ゾンビ（6/12処理済）。
- `@anthropic-ai` localhost ガードは **Mac も masup も未設定**（`npm view` が版を返す＝ECONNREFUSEDしない）。スキルの「3台統一済み」は実態と相違。

### 実施（すべて検証済み）
1. npm-global `@anthropic-ai/claude-code` を uninstall → claude は standalone 単一に（`where claude`=`.local\bin` のみ）。`claude update` で config install method を global→**native へ正常化**（SSH越し更新が一時 global に書換えていたのを是正）。
2. Windows-native npm `@openai/codex`（0.140.0）を uninstall（`where codex`(Win)=なし、WSL2 0.141.0 は温存）。
3. task `codex-remote-control` 削除 ＋ task `codex-watchdog`（無効）削除。
4. `C:\ProgramData\codex-watchdog\` を `C:\Users\gci_admin\codex-watchdog-removed-20260619\` へ退避（バックアップ兼掃除、`Test-Path`=False）。
5. **ガードは追加せず**（Mac も未設定＝足すと逆に不統一。両機とも「claude を npm で入れない」運用で統一）。
6. **WSL2 remote-control（task `CodexWSLRemoteControl`／PID 148367）は意図的に温存**＝ユーザー選択。

### 最終状態（検証）
- 残存タスク（codex/claude）= `Claude Code Update` ＋ `CodexWSLRemoteControl` のみ
- claude 2.1.181（standalone単一・config=native）/ WSL2 codex 0.141.0（単一）/ gemini 0.47.0
- WSL2 remote-control 稼働継続

### 要追記
- [x] ~~スキル `updating-ai-cli-fleet` の「@anthropic-ai localhost ガード3台統一済み」記述が実態と相違~~ → **6/19 修正済み**（「絶対に守ること」の該当行を実態へ書換え＝「claude は npm で入れない／standalone のみ、ガードは現状どこにも無い、npm-global に claude を見つけたら除去」。ユーザー「1だけ対応」指示）。
- Codex Desktop App（MSIX）の `Codex.exe` が **10プロセス・計~800MB 稼働中**（共有機・8GB）。委譲は CLI(WSL2) で足りるので、GUI 常駐の要否は別途判断材料（今回は未介入）。

## 持ち越し

- [ ] **しゅん先生 PC の3CLI更新** — 今回は `desktop-atq36ks.local` 名前解決不可＝電源オフ/mDNS圏外（masup は到達したので同一LAN外＝コワーキング据え置き機が落ちている）。次回コワーキング着席時に実施。Codex は WSL ではなく Windows 運用なら旧ピン要再確認だが、masup 同様 WSL 化しているか現地確認が先
- [x] ~~masu-p55 の二重インストール整理（上記気づき1・2）~~ → **6/19 実施・解決**（上記「追記」セクション）

## 追記2: おときた駿「ClaudeでPython自動化」ツイート リアリティチェック（fact-checking-x-posts）

対象: https://x.com/otokita/status/2065986307416051881 （6/14、いいね228/24,461view）。本文「ClaudeとPythonで政治ニュース・国会議事録から特定話題を毎日自動で拾って要約する仕組みを30分で作った。チャットで聞きながら作ればコード知識ゼロで可能」。ユーザー指示＝「リアリティチェック」。

**総合判定: ⚠️ 概ね本当**（コア＝「Claude+Python でこの仕組みは作れる」は✅。盛りは「30分・コード知識ゼロ」が"動く初版"の話で、"毎日自動運用に耐える版"の工程を省略している点）。

- ①仕組みの実現性 → ✅ 事実。国会議事録は公式「国会会議録検索システム 検索用API」(kokkai.ndl.go.jp/api.html、JSON/XML・無料・認証不要・HTTP GET、`/api/speech` 等)で機械取得可。既存 MCP/Qiita 実例多数。ニュース=RSS/API、要約=Claude API、毎日=scheduler。**当方の job-search-daily（毎朝04:30 LaunchAgent）が同型を実運用中＝実機で成立確認済み**。
- ②所要30分 → ⚠️ 初版なら妥当。但し本番日次は別物: job-search-daily は6/10だけで5回改訂＋実在/勤務地2段検証、6/12-13 で silent failure（0件でも push/メール、失敗握り潰し）根治、TCC罠でスケジューラ exit126 即死。
- ③コード知識ゼロ → ⚠️ 初版作成は可。詰まるのは環境構築・スケジューラ・**壊れた時のデバッグ**（日次自動化は静かに壊れる: 認証揮発/0件/文字化け/TCC）。
- 補足: おときたは課金煽り系でなく素直な驚き。gap は普遍的な「デモ↔本番」差で、当方が最も知る領域。

一次ソース: 国会会議録検索システム 検索用API仕様 https://kokkai.ndl.go.jp/api.html ／ 実例 https://qiita.com/sanskruthiya/items/8da9186f1764ae6b6e3a

## 追記3: 毎朝04:30 求人メール不具合のデバッグ＆修理（systematic-debugging）

ユーザー報告「毎朝04:30のメールがおかしい」。Phase1で根本原因を特定→フル復旧→検証。

**症状**: 毎朝、件名「⚠️2026-06-17分の再送(当日分未生成)・計?件」＋本文が求人一覧でなく「MINE TIME 板宿駅前店」インカフェのメモ、という誤メールが届いていた。

**根本原因（確定）**: `job-search-daily.py` の push 手順（259行目）で **`git pull --rebase` が `git add`/commit より前**に実行されていた。`job-hunt-reports` 作業ツリーに 6/13 08:47 のテスト実行が `reports/job-report-2026-06-13.md` を上書きした未sta変更が残留 → pull が rc=128（"cannot pull with rebase: You have unstaged changes"）で失敗。6/12-13 の堅牢化で git 失敗が FATAL 化していたため、**6/14〜6/19 の6日間、生成は成功（total 86〜97）するが push と EMAIL_TRIGGERED 直前で毎日中断**（launchctl 終了コード=1）。公開repoは6/13で止まり、別途手動で置かれた `job-report-2026-06-17.md`（中身=MINEメモ）が混入。05:00バックアップ送信が公開repoの"latest"＝この誤6/17を拾い、毎朝それを送っていた。

**修理（フル復旧、ユーザー選択）**:
1. スクリプト堅牢化: 順序を `add → commit → pull --rebase --autostash → push` に並べ替え（git正本 `scripts/job-search-daily-mac.py` ＋本番 App Support に同期、py_compile OK）。作業ツリーに残骸が1つあっても二度と wedge しない。
2. 公開repo復旧: 6/13テスト残骸を `git restore`、誤6/17を実レポートで差し替え、6/14-19 の実レポート6日分を追補 push（MINEメモ本体は `docs/projects/mine-time-itayado-2026-06-16.md` に既存＝消失なし）。
3. 検証: 堅牢化スクリプトを `JS_NO_EMAIL=1` で実走 → `REPORT_OK→PUSHED→EXIT=0`（FATAL消滅）。公開repo に 6/14-19 が全て揃い、6/17 が実レポート化を確認。

**残/フォロー**:
- 今日の修正版メールを今すぐ送るかはユーザー判断（`last-emailed-date.txt`=6/16 のため通常送信で再送可）。明日04:30からは EMAIL_TRIGGERED が正常動作＝自然復旧。
- 候補: `launchagent-doctor` に「最終終了コード非0」検出を追加（今回 launchctl status=1 が6日続いたが気づけなかった。終了コード監視があれば即検出できた）。

**教訓**: 「サイレント失敗の握り潰しを止めて FATAL 化」（6/12-13）は正しかったが、**git 同期手順自体が作業ツリーの汚れに弱い設計（pull-before-commit）**だった。FATAL 化は"沈黙"を"停止"に変えただけで、根本の脆さは別途直す必要があった。テスト実行（`JS_NO_PUSH=1`）が作業ツリーにファイル変更を残す点も誘因。

## 関連

- スキル: @skills/updating-ai-cli-fleet/SKILL.md
- 機材: @docs/machines/masu-p55.md（バージョン行が 6/13 のまま＝要更新）
- ピン留め原典: @docs/journal/2026-05-20.md / 2026-05-25.md

## タグ

#CLIアップデート #claude2181 #codex0141 #gemini047 #2台統一 #ピン留め失効 #WSL2移行 #しゅん先生PC持ち越し #二重インストール #configドリフト
