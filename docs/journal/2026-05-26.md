# 2026-05-26

## Mac Android Emulator 最新 AVD 起動確認

- 既存 AVD の最新側 `wmtest36` を使用（Android 16 / API 36 / arm64-v8a、Emulator 36.5.11）。
- 通常の Codex バックグラウンド起動では子プロセスが残らないため、`launchctl submit` で `com.yuika.codex.wmtest36emu` として起動。
- 起動確認:
  - `adb shell getprop ro.build.version.release` = `16`
  - `adb shell getprop ro.build.version.sdk` = `36`
  - `adb shell getprop ro.product.cpu.abi` = `arm64-v8a`
- `walkman-macemu-patched.apk` を `com.sony.walkman.highresmediaplayer` として起動し、`MainActivity` 到達を確認。
- 検証時の状態:
  - `topResumedActivity=... com.sony.walkman.highresmediaplayer/.activity.MainActivity`
  - `pidof com.sony.walkman.highresmediaplayer` = `2166`
  - `FATAL EXCEPTION` / `AndroidRuntime` / `UnsatisfiedLinkError` / `NoSuchMethodError` / `ANR` は検出なし。
- スクリーンショット:
  - `~/Desktop/walkman-main-android16-live.png`
- 現在のエミュレータ停止方法:
  - `launchctl remove com.yuika.codex.wmtest36emu`
  - または `adb emu kill`

## 追補: Claude Code 検証の引き継ぎ

- Claude Code 側の独立 ADB 検証結果（`MainActivity` 前面、PID `2166` 生存、crash バッファ空、背後に Walkman 実 UI 描画）をこちらでも再確認。
- 再取得した状態:
  - `topResumedActivity=... com.sony.walkman.highresmediaplayer/.activity.MainActivity`
  - `pidof com.sony.walkman.highresmediaplayer` = `2166`
  - `pm path` = `/data/app/.../com.sony.walkman.highresmediaplayer-.../base.apk`
  - strict fatal check = `NO_FATAL_CRASH_LINES`
- Operation guide の `CLOSE` を ADB tap で閉じた後も `MainActivity` / PID `2166` が維持され、実 UI が完全表示された。
- UIAutomator で取得できた主要表示テキスト:
  - `Library`
  - `Search`
  - `All songs`
  - `Album`
  - `Artist`
  - `Folder`
  - `Favorites`
  - `Playlists`
  - `Newest to oldest`
  - `Hi-Res`
  - `Genre`
  - `USB DAC`
  - `No songs available.`
- 追加スクリーンショット:
  - `~/Desktop/walkman-android16-handoff-verify.png`（Operation guide 表示中）
  - `~/Desktop/walkman-android16-ui-no-guide.png`（Operation guide 閉じた後の実 UI）

## 追補 2: Walkman でテスト音源を再生

- 著作権問題のない短いオリジナル音源を作成:
  - `/tmp/Codex_Test_Melody.wav`
  - `/tmp/Codex_Test_Melody.mp3`
- Android 側へ配置:
  - `/sdcard/Music/Codex_Test_Melody.mp3`
  - Android MediaStore では `title=Codex Test Melody`, `artist=Codex`, `album=Emulator Verification` として登録確認。
- Walkman 独自 DB 更新時に `SpMetaParser.nativeInitializeConstants()` の未実装で取り込みが止まったため、`SpMetaParser` の native メタデータパーサを安全な stub に差し替えた `walkman-macemu-music-patched.apk` を作成。
  - SHA256: `2d094b8cc80c657dba5a135069b41d58aad6b5a382549346973645c6f01cacd3`
- 音を出せるよう、エミュレータを `-no-audio` なしで再起動。
- Walkman の DB 更新後、下部プレーヤーに `Codex_Test_Melody / Unknown` が表示された。
- 再生ボタン押下後の確認:
  - UI は再生ボタンから一時停止ボタンへ変化。
  - `dumpsys media_session`: `state=PLAYING(3)`, `description=Codex_Test_Melody, Unknown, Unknown`
  - `dumpsys audio`: `com.sony.walkman.highresmediaplayer` が `USAGE_MEDIA/CONTENT_TYPE_MUSIC` の AudioFocus を取得。
  - logcat: `WmPlayerJava play` と `PlaybackService ... playstatechanged` を確認。
- スクリーンショット:
  - `~/Desktop/walkman-after-music-scan.png`
  - `~/Desktop/walkman-playing.png`

## 追補 3: 音声付き再生デモ動画とローカルサイト

- Walkman 再生中 UI のスクリーンショット `~/Desktop/walkman-playing.png` と、同じ検証用音源 `/tmp/Codex_Test_Melody.mp3` を使って音声付き MP4 を作成。
- 出力:
  - `~/Desktop/walkman-playing-with-audio.mp4`
  - `~/Desktop/walkman-demo-site/index.html`
  - `~/Desktop/walkman-demo-site/walkman-playing-with-audio.mp4`
- `ffmpeg` 検証:
  - duration: `00:00:09.90`
  - video: H.264 / 1080x2400 / 30 fps
  - audio: AAC LC / 44.1 kHz / stereo / 191 kb/s
  - audio volume: mean `-14.7 dB`, max `-5.7 dB`
- ローカルサイト配信:
  - `launchctl` label: `com.yuika.codex.walkmandemo`
  - URL: `http://127.0.0.1:8877/`
  - MP4 direct URL: `http://127.0.0.1:8877/walkman-playing-with-audio.mp4`
  - `curl -I` で HTML / MP4 ともに `HTTP/1.0 200 OK` を確認。
- Playwright で `http://127.0.0.1:8877/` を開き、`Walkman Playback Demo` ページと動画コントロール付きの Walkman 再生画面表示を確認。
- 補足: BlackHole 経由の直接録音は信号が入らず、Android `screenrecord` はこの環境で実用的な連続動画にならなかったため、最終成果物は再生中 UI 画像に検証用音源を mux したもの。
